i have created this script in a wrong way so i will re do my assignment
again and  will upload again
 
Net-tools Installation script
$ sudo apt update
$ sudo apt install net-tools
$ which ifconfig
$ ifconfig

Telnet Installation script
$ sudo apt update
$ sudo apt install telnet

Puppet Installation script
$ sudo apt update
$ sudo apt install puppet
$ puppet --version
$ which puppet

Ansible Installation script
$ sudo apt update
$ sudo apt install ansible
$ ansible --version
$ which ansible

Echo
$ Echo "Hello World"
